# Helm PRD

The product lets a developer keep a live terminal view open while harness planning files change.

Telemetry is required so the app can surface refresh timing and parse-health instrumentation.
Feature flags are required for rolling out advanced panes safely.
