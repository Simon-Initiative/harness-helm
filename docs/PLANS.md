# Plans

## Work Item Model

Active work lives under `docs/exec-plans/current/`.

Each work item is expected to contain a feature directory with source-of-truth planning artifacts such as:

- `prd.md`
- `fdd.md`
- `plan.md`
- `requirements.yml`

The product focus of this repository is to make those files observable as a living state model inside the terminal.
